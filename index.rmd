---
title: "Erick David Rodriguez-Orduna site"
output:
        html_document:
                df_print: paged
---

[comment]: <> (title: "Erick David Rodriguez-Orduna site")

[comment]: <> (output:)

[comment]: <> (html_notebook: default)

Hello and welcome to my website :D

Here I will include my work as a PhD student @[UDLAP](https://www.udlap.mx/web){target="_blank"}

The code chunk appears:
```{r}
```

Type any R code in the chunk, for example:
```{r}
mycars <- within(mtcars, { cyl <- ordered(cyl) })
mycars
```

Now, click the **Run** button on the chunk toolbar to [execute](https://www.jetbrains.com/help/pycharm/r-markdown.html#run-r-code) the chunk code. The result should be placed under the chunk.
Click the **Knit and Open Document** to build and preview an output.


```{r}
x= c(1,2,3,4)
y= c(2,2,2,5)
plot(x,y)
lines(x,y)
```
```{r}
plot(y,x)
lines(y,x)
```

```{r}
```